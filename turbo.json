{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build", "^types"]
        },
        "dev": {
            "cache": false,
            "persistent": true
        },
        "test": {},
        "types": {},
        "typecheck": {},
        "deploy": {
            "dependsOn": ["^build"],
            "passThroughEnv": ["CLOUDFLARE_API_TOKEN"],
            "cache": false
        },
        "publish-release": {
            "dependsOn": ["^build", "^types", "^typecheck", "^test"],
            "cache": false
        }
    }
}
